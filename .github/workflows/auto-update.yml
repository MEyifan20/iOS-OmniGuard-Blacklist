name: 🚀 捕食者自动化构建 | Predator Auto Build

on:
  push:
    paths:
      - 'iOS-OmniGuard-Blacklist.txt'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-sync:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 配置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 🛠️ 运行脚本 (强制 UTF-8)
        env:
          PYTHONUTF8: 1
        run: python scripts/update_rules.py

      - name: 📤 提交并推送
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "github-actions[bot]"
          git add iOS-OmniGuard-Blacklist.txt changelog.md README.md
          if git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "chore: 自动化联动更新 [skip ci]"
            git push
          fi
